<h1>Bands</h1>

<ul>
    <% Band.all.each do |band| %>
        <li><%= link_to band.name.capitalize ,band_url(band) %></li>
    <% end %>
</ul>

<% if admin? %>
<%= link_to 'New Band', new_band_url %>
<% end %>